export interface IStudentElementModel {
  name: string;
  id: number;
  grade: number;
  subject: string;
  email: string;
  date: `${number}-${number}-${number}`;
  address: string;
  city: string;
  country: string;
  zip: number
}

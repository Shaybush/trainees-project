import { BackgroundByAverageDirective } from './background-by-average.directive';

describe('BackgroundByAverageDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundByAverageDirective();
    expect(directive).toBeTruthy();
  });
});
